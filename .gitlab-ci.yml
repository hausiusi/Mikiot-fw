variables:
  BUILDER_SCRIPT: "mikiot-build.sh"

build:
  image: docker:latest
  stage: build
  script:
    - echo '--- PRINT USERNAME ----'
    - whoami
    - echo '-----------------------'
    - $BUILDER_SCRIPT
    - echo "Exit code "$?